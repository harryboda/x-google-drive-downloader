# Google Drive Downloader 环境变量配置示例
# 复制此文件为 .env 或直接设置系统环境变量

# Google OAuth 2.0 凭证
# 从 Google Cloud Console 获取这些值
GOOGLE_CLIENT_ID=your_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_client_secret

# 安全提示:
# 1. 永远不要将真实的凭证提交到代码仓库
# 2. 将 .env 文件添加到 .gitignore 中
# 3. 生产环境使用更安全的密钥管理服务

# 使用方法:
# 1. 复制此文件: cp .env.example .env
# 2. 编辑 .env 文件，填入真实的凭证
# 3. 在终端中运行: source .env
# 4. 然后运行应用: flutter run -d macos